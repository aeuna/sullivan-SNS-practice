import { Card, CardContent } from "@material-ui/core";

const edit = () => {
  return (
    <div>
      <Card variant="outlined">
        <CardContent>{/*  UploadForm을 추가하세요. */}</CardContent>
      </Card>
    </div>
  );
};

export default edit;
