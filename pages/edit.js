import React from "react";
import { Card, CardContent } from "@material-ui/core";

const edit = () => {
  return (
    <div>
      <Card variant="outlined">
        <CardContent>{/*  UploadForm 컴포넌트를 추가해주세요. */}</CardContent>
      </Card>
    </div>
  );
};

export default edit;
